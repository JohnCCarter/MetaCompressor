# Documentation index

| Folder | Role |
|--------|------|
| **`policy/`** | Layout policy, structure cleanup backlog, working contract (`METACOMPRESSOR_WORKING_CONTRACT.md`). |
| **`planning/`** | Roadmaps, phased rollout, performance and adoption notes. |
| **`differential/`** | Differential compression design, checklists, slice plans and reports. |
| **`adr/`** | Accepted architecture decisions (`0001-…`, index in `adr/README.md`). |

Governance order for agents: **`AGENTS.md`** at repo root, then **`docs/policy/`** as listed there.

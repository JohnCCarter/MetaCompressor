# Ingen indexerings-exkludering: inga mönster nedan ⇒ Cursor/AI kan indexera hela arbetsytan.
# Lägg till rader här endast om du vill begränsa igen senare (samma syntax som .gitignore, inkl. `!` för undantag).
